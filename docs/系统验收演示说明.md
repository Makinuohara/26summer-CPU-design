# 进阶层次完整计算机系统验收演示说明

## 1. 演示目标

本演示用同一套 RV32I 汇编程序依次证明以下功能已经形成完整、可运行的计算机系统：

1. 五级流水线 CPU 能正确执行算术、逻辑、移位、分支、跳转和访存指令。
2. 指令存储器能加载并连续运行一个超过简单 smoke test 的程序。
3. 数据存储器与直接映射 Cache 能完成写入、命中读取和冲突重填。
4. CPU 能通过 MMIO 访问拨码开关、LED、八位数码管和 PS/2 控制器。
5. PLIC 能完成外设中断仲裁，CPU 能执行 `claim → ISR → complete → mret`。
6. 性能计数器能量化 cycle、instret、stall、flush、CPI 和吞吐量。

板上主流程只依赖拨码开关、LED 和数码管。PS/2 键盘是加分项，键盘不可用时不影响前六项结论。物理按键当前在 `fpga_top` 中接为 `5'b0`，因此不作为验收输入。

## 2. 文件与构建

| 内容 | 文件 |
|---|---|
| 演示汇编源码 | `sim/system_acceptance_demo.asm` |
| 指令映像 | `sim/system_acceptance_demo.hex` |
| 自动验收 testbench | `sim/tb_system_acceptance_demo.v` |
| 专用 FPGA 顶层 | `src/fpga_acceptance_top.v` |
| Vivado 构建脚本 | `scripts/vivado_build_acceptance.tcl` |

### 2.1 重新生成程序映像

在仓库根目录运行：

```powershell
python scripts\asembler.py sim\system_acceptance_demo.asm sim\system_acceptance_demo.hex
```

正常输出：

```text
Done: 299 instructions → sim/system_acceptance_demo.hex
```

### 2.2 自动化仿真

Linux/WSL：

```bash
iverilog -g2012 -I src/memory -s tb_system_acceptance_demo -o /tmp/tb_system_acceptance_demo.vvp -c scripts/filelist_acceptance.f sim/tb_system_acceptance_demo.v
vvp /tmp/tb_system_acceptance_demo.vvp
```

通过时最后会输出四组性能数据和：

```text
PASS: complete system acceptance demo passed
```

### 2.3 生成 bitstream

```powershell
& 'D:\Vivado\Vivado\2018.3\bin\vivado.bat' -mode batch -source scripts\vivado_build_acceptance.tcl
```

输出文件：

- `build/bitstreams/system_acceptance_demo.bit`
- `build/bitstreams/system_acceptance_timing.rpt`
- `build/bitstreams/system_acceptance_utilization.rpt`

烧录时选择 `system_acceptance_demo.bit`。烧录属于外部硬件操作，执行前确认开发板型号为 Nexys 4 DDR、器件为 `xc7a100tcsg324-1`。

## 3. 操作规则

- `SW[15:12]`：功能页编号，范围 0–7。
- `SW[11:0]`：该页面的输入数据。
- 数码管最左一位显示页编号，后续位显示输入或计算结果。
- 每次拨动开关都会触发 PLIC 源 10 中断。ISR 读取开关快照并返回，主程序再根据快照执行对应页面。
- 页面切换后等待数码管稳定再讲解。CPU 为方便观察被分频到约 381 Hz，复杂页面可能需要短暂等待。

建议验收前把所有开关拨到 0，按下低有效复位键后释放。

## 4. 逐步验收流程

### 步骤 1：上电自检——CPU、流水线和存储基本通路

操作：

1. 将 `SW=0x0000`。
2. 复位并释放。
3. 等待数码管稳定。

预期：

```text
SEG = 600DC0DE
LED = FFFF
```

讲解：程序实际执行了加、减、异或、移位、有符号比较、条件分支以及 DMEM 写后读。`600DC0DE` 是成功签名，16 个 LED 全亮表示所有自检均通过；失败时显示 `DEADC0DE` 且 LED 熄灭。

### 步骤 2：MMIO 镜像——输入、地址译码和输出

操作：依次拨成 `SW=0x1123`、`0x1A5A`。

预期：

| SW | SEG | LED |
|---|---|---|
| `1123` | `10001123` | `1123` |
| `1A5A` | `10001A5A` | `1A5A` |

讲解：CPU 用 `lw` 从 `0x80000008` 读取开关，经 MMIO 总线译码后用 `sw` 写 `0x8000000C` 的 LED，并逐位写数码管寄存器。输入和输出一一对应，证明不是硬编码显示。

### 步骤 3：ALU 数据通路——可复核的实时计算

本页取低 8 位 `x`，计算：

```text
y = (((x + 3) XOR 0x5A) << 1) AND 0xFF
```

数码管格式为 `2000XXYY`，LED 低 8 位显示 `YY`。

| SW | x | 预期 y | SEG | LED |
|---|---:|---:|---|---|
| `2012` | `12` | `9E` | `2000129E` | `009E` |
| `2020` | `20` | `F2` | `200020F2` | `00F2` |

讲解：该表达式连续覆盖加法、异或、左移、按位与和流水线数据前递。

### 步骤 4：DMEM 与 Cache——写入、命中和冲突重填

操作：设置 `SW=0x3005`。

程序行为：

1. 向地址 `0x00/0x04/0x08/0x0C` 写入 `5/6/7/8`。
2. 连续读回同一 Cache 行并求和，得到 `0x1A`。
3. 访问同 Cache 索引、不同 tag 的 `0x100`，制造直接映射冲突。
4. 再次读取地址 0，验证冲突重填后的数据仍正确。

预期：

```text
SEG = 3000001A
LED = FFFF
```

讲解：当前 Cache 为 16 行、每行 4 字、直接映射、写直达、写不分配。板上现象证明最终存储语义，自动仿真进一步检查访问路径和 stall。

### 步骤 5：小型程序——Fibonacci 完整运行

操作：设置 `SW=0x400A`，即计算 `fib(10)`。

预期：

```text
SEG = 400A0037
LED = 0037
```

其中十六进制 `0x37` 等于十进制 55。

讲解：该页不是单条指令测试，而是包含循环、条件分支、迭代状态和数据相关的小型程序，证明 CPU 能完整运行有实际控制流的程序。

### 步骤 6：流水线压力程序——性能瓶颈

操作：设置 `SW=0x5000`。

程序依次运行：

1. ALU/前递循环。
2. `lw` 后立即使用的 load-use 循环。
3. 连续 taken branch 循环。
4. 地址 0 与 `0x100` 交替读取的 Cache 冲突循环。

LED 低五位全亮表示各阶段完成；数码管显示 `5` 开头的 checksum。精确指标由仿真在 DMEM 标记 `0x51–0x55` 之间截取，不依赖人工数周期。

当前自动仿真的一组结果：

| 区间 | cycle | instret | CPI | stall | flush |
|---|---:|---:|---:|---:|---:|
| ALU/前递 | 303 | 84 | 3.60 | 204 | 15 |
| load-use | 468 | 84 | 5.57 | 369 | 15 |
| taken branch | 204 | 51 | 4.00 | 138 | 15 |
| Cache 冲突 | 456 | 52 | 8.76 | 397 | 7 |

不同 RTL 版本修改后应重新运行仿真，以新输出为准。

### 步骤 7：PLIC 和外部中断

操作：设置 `SW=0x6000`，随后轻拨任意低位开关再拨回。

数码管格式：

```text
6 00A CCCC
```

- `A` 表示最近一次 claim ID 为十进制 10，即十六进制 `A`。
- `CCCC` 是累计开关中断次数，会随每次拨动增加。
- LED0 始终亮，表示 ISR 返回后主循环仍在运行。

讲解顺序：开关同步器检测变化 → 源 10 pending → PLIC 根据 priority/enable/threshold 拉高 `meip` → CPU 排空流水线并跳到 `mtvec=0x800` → ISR 保存寄存器、claim、读取开关清源、complete → `mret` 返回。

### 步骤 8：PS/2 键盘中断（可选加分）

操作：

1. 接好 PS/2 键盘后设置 `SW=0x7000`。
2. 依次按 `A` 和 `S`。

数码管格式为 `7-中断次数-扫描码`。典型扫描码：

| 按键 | 扫描码 |
|---|---|
| A | `1C` |
| S | `1B` |

程序通过 PLIC 源 2 接收键盘中断。若现场键盘桥接链路首次输入不稳定，先连续按两次再观察；仍不可用时立即回到页面 6，以已通过的自动 PS/2 仿真作为证据，不让可选外设影响主验收。

## 5. 性能量化方法

### 5.1 板载演示频率

`clk_div` 输出计数器最高位，当前 `CLK_DIV_BITS=18`：

```text
Fdemo = 100 MHz / 2^18 ≈ 381.47 Hz
```

这是为了让人眼能稳定观察程序状态而主动选择的演示频率，不是流水线 CPU 的极限频率。

### 5.2 CPI 与吞吐量

对每个 benchmark 区间取性能计数器差值：

```text
CPI = Δcycle / Δinstret
实现极限吞吐量 = Fmax / CPI
板载演示吞吐量 = 381.47 / CPI  指令/秒
```

例如当前 Cache 冲突区间 `CPI=8.76`，板载分频下约为：

```text
381.47 / 8.76 ≈ 43.5 指令/秒
```

验收构建额外使用 `constraints/acceptance_cpu_timing.xdc`，在不改变板上 18 位分频硬件的前提下，以 100 MHz 目标约束 CPU 时钟域，避免 CPU 路径被静态时序分析遗漏。本次 Vivado 2018.3 实现后结果为：

```text
CPU 目标周期 = 10.000 ns
CPU WNS      = +0.169 ns
TNS          = 0
未约束最大延迟端点 = 0
```

因此可以保守地陈述“当前实现通过 100 MHz CPU 域静态时序检查”。按关键路径 `10 - 0.169 = 9.831 ns` 估算，临界 Fmax 约为 `101.72 MHz`；由于没有进行多轮时钟约束搜索，正式验收以“100 MHz 已通过”为结论，不把 101.72 MHz 宣称为已验证工作频率。

按已通过的 100 MHz 目标估算各 workload 的吞吐量下限：

| 区间 | CPI | 100 MHz 下吞吐量 |
|---|---:|---:|
| ALU/前递 | 3.60 | 27.78 MIPS |
| load-use | 5.57 | 17.95 MIPS |
| taken branch | 4.00 | 25.00 MIPS |
| Cache 冲突 | 8.76 | 11.42 MIPS |

实现后资源占用为 3343 个 LUT（5.27%）、5120 个寄存器（4.04%）、8 个 Block RAM Tile（5.93%）、0 个 DSP。没有完整且已约束的 timing report 时只报告演示频率和 CPI，不猜测 Fmax。

### 5.3 瓶颈和优化建议

| 现象 | 原因 | 优化方向 |
|---|---|---|
| load-use CPI 上升 | load 数据到达前相关指令必须暂停 | 更低延迟存储、load 数据提前旁路 |
| 分支产生 flush | 分支在后级确定，错误路径指令被清空 | 提前分支判定、静态或动态分支预测 |
| Cache 冲突 CPI 最高 | 直接映射同索引地址反复替换 | 增加容量/相联度、预取、增加 hit/miss 计数器 |
| 板上运行较慢 | 为可观察性使用 18 位分频 | 完成时序收敛后减小分频或直接使用更高 CPU 时钟 |

仓库当前没有可用的基础单周期 CPU RTL，因此不能给出同源、同工具、同程序的单周期 Fmax 对比。单周期 `CPI≈1` 只能作为理论参照，验收中应明确这一限制，不使用虚构数据。

## 6. 现场故障预案

| 现象 | 检查和恢复 |
|---|---|
| 数码管无显示、LED 全灭 | 确认烧录的是 `system_acceptance_demo.bit`；将 SW 清零并重新复位 |
| 显示不是 `600DC0DE` | 确认 hex 被加入 Memory Initialization Files，检查 Vivado 日志中的 `$readmemh` 路径 |
| 页面切换较慢 | 等待当前 ISR 和八次数码管 MMIO 写入完成，不要连续快速拨动 |
| 页面 6 计数不是固定值 | 正常；之前每次切页本身都会产生一次开关中断，只检查计数递增和 claim ID 为 A |
| PS/2 无响应 | 连续输入两次；仍失败则使用页面 6 和自动仿真完成主验收 |
| 老师询问 Cache 是否真的命中 | 展示 testbench 的四个性能区间，并指出冲突访问 CPI 明显高于普通 ALU 和 load-use 区间 |

## 7. 一分钟总结话术

“这不是把 LED 直接接到开关，而是 CPU 运行 299 条指令组成的汇编映像。开关变化先通过 PLIC 中断进入 CPU，ISR 保存现场并读取 MMIO，主程序再执行 ALU、存储、Cache 冲突和 Fibonacci 等不同任务，最后通过 MMIO 驱动 LED 和八位数码管。仿真同时读取流水线内部 cycle、instret、stall、flush 计数，计算出不同 workload 的 CPI。结果表明 load-use、分支清空和直接映射 Cache 冲突是主要瓶颈，对应的优化方向分别是旁路、分支预测和提高 Cache 相联度。”
